import Link from "next/link"; import { TestimonialManager } from "@/features/testimonials/testimonial-manager"; import { getAdminTestimonials } from "@/server/admin/testimonials"; import { requireTeamAdmin } from "@/server/auth/current-user";
export default async function TestimonialsPage() { await requireTeamAdmin(); const testimonials = await getAdminTestimonials(); return <div><Link className="text-sm font-semibold text-white/45 hover:text-white" href="/admin">← Team dashboard</Link><p className="mt-8 text-xs font-bold tracking-[0.16em] text-[#ffc83d] uppercase">Team administration</p><h1 className="mt-3 font-[family-name:var(--font-display)] text-4xl font-bold tracking-[-0.04em] sm:text-5xl">Testimonials</h1><p className="mt-3 max-w-3xl leading-7 text-white/55">Create drafts, control publication, and order approved testimonials.</p><TestimonialManager testimonials={testimonials} /></div>; }
